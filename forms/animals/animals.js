/*
let myAnimals = ["dog", "cat", "horse", "meerkat"]

let newAnimal = prompt("Enter the name of a new animal.")

var lowerCaseNewAnimal = newAnimal.toLowerCase();

myAnimals.push(lowerCaseNewAnimal)

    alert(`The Last Animal is ${myAnimals.slice(-1)}`)
    
    }
  */